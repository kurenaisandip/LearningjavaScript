// Scope determines the accessibility (visibility) of variables.

// JavaScript has 3 types of scope:

// Block scope
// Function scope
// Global scope


// do not clear global variables unless we intent to
// global variable (or function) can overwrite window variables (or functions)
/// any function, including the window objetc can overwrite our global variables and functions.

// the lifetime of js variables

// the lifetime of js variables starts when it is declared. 

// function(local) variables are deleted when the function is completed.

// in a web browser, global variables are deleted whenn we close the browser or tab.

// functional arguments(parameters) work as local variables insode functions.

